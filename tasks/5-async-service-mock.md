## 5 async service mock

- Add dependencies for `nav-bar.component.ts` with mocks and `RouterTestingModule`
- Create `Subject` and mock service calls with Subject
- mock utils with `jest.spyOn`
- add tests for all functions
